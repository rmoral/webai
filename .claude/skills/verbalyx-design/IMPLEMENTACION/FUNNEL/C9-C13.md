# C9 · `/precios` (D2)

**Archivos:** `components/marketing/pricing-plans.tsx`, `app/[locale]/(marketing)/pricing/page.tsx`, `lib/usage/summary.ts` (una consulta nueva), `messages/*.json`.

**Selector:** `useState<BillingInterval>("yearly")`. Borra el comentario que defiende Mensual: la línea bajo el selector es lo que evita esconder la prueba.
- En Anual, junto al selector, «Ahorra 50 % (seis meses gratis)» en `text-success-ink`. Debajo: «¿Prefieres probar antes? La prueba gratis de 3 días está en Ilimitado mensual.» + un botón-enlace «Ver precios mensuales» que cambia el ciclo.
- En Mensual, debajo: «Pagando al año ahorras un 50 % (seis meses gratis), sin prueba gratuita.» + «Ver precios anuales».
- `pricing_view` se sigue emitiendo una vez, con `cycle: "yearly"` salvo que la URL traiga `?cycle=monthly` (los muros que ofrecen la prueba deberían enlazar así).

**Tarjeta:**
- Precio grande: `monthlyEquivalent`. Debajo, en tinta y `font-medium`: `yearlyTotal` («89,88 US$ al año»), con el hueco reservado en mensual para que las tarjetas no salten.
- Luego la frase de cobro: «Se cobran 89,88 US$ hoy, una vez al año.»
- Se quita el «Ahorras 90,00 US$» de la frase: el ahorro ya está junto al selector.

**Insignias y CTAs:**
- `badge="Más popular"` en Ilimitado, en los dos ciclos. Se va `trialBadge`: la prueba se dice en el CTA («Probar 3 días gratis») y en la frase de cobro.
- Un único `Button` relleno, el de Ilimitado. Pro pasa a `variant="outline"`, y Gratis ya lo era.

**Móvil:** en una columna, Ilimitado va primero (`order-first` por debajo de `sm`). Selector a todo el ancho y botones de 44 px como mínimo.

**Franjas bajo las tarjetas** (dos columnas; una sola si no hay dato de prueba social):
- Prueba social: `{n} de palabras procesadas en Verbalyx desde {mes año}`. Añade `getWordsProcessed()` en `lib/usage/summary.ts`: `select coalesce(sum(words_in),0) from usage_daily` y `min(date)`. La página es estática, así que se hace en build o con `revalidate = 86400`. Formato: redondea **hacia abajo** a una cifra decimal («12,4 millones»; por debajo de un millón, a miles: «840.000»). Si la suma es menor de 100.000 o la consulta falla, la franja no se pinta.
- Cancelación: el `guarantee` que ya existe sube aquí, en `success-soft`: «**Cancela online en dos clics**, cuando quieras: Mi cuenta → Suscripción → Cancelar. Sin llamadas ni correos.»

**Con sesión** (se resuelve en el navegador, como `HeaderAuth`):
- Gratis → en su tarjeta, un recuadro sin acción «Tu plan actual», en lugar de «Crear cuenta gratis» (esto cierra también C12 en esta página).
- Pro → su tarjeta dice «Tu plan actual». Ilimitado ofrece «Cambiar a Ilimitado» → `/app/account` (portal), porque `/checkout` rechaza `already_subscribed`. Aviso: «El cambio se hace desde Mi cuenta. Antes de confirmar verás el importe exacto que se ajusta en tu próxima factura.» Sin línea de prueba bajo el selector.
- Ilimitado → «Tu plan actual» en Ilimitado y «Cambiar a Pro» (outline) → `/app/account`.

**Impuestos:** `taxNote` y `faq[4]` ya dicen «incluidos». **Reescribe `legal.terms` → «Precios e impuestos»**, que dice lo contrario (texto legal: que lo revise quien corresponda).

**Claves (`pricing.*`):**
- Cambian `twoMonthsFree` → `"saveHalf": "Ahorra 50 % (seis meses gratis)"`, y `billingProYearly` / `billingUnlimitedYearly` → `"Se cobran {amount} hoy, una vez al año."`.
- Se añaden:
  - `"mostPopular": "Más popular"`
  - `"trialHint": "¿Prefieres probar antes? La prueba gratis de {days} días está en Ilimitado mensual."`
  - `"seeMonthly": "Ver precios mensuales"`
  - `"yearlyHint": "Pagando al año ahorras un 50 % (seis meses gratis), sin prueba gratuita."`
  - `"seeYearly": "Ver precios anuales"`
  - `"proof": "{words} de palabras procesadas en Verbalyx desde {since}."`
  - `"currentPlan": "Tu plan actual"`
  - `"switchToUnlimited": "Cambiar a Ilimitado"`
  - `"switchToPro": "Cambiar a Pro"`
  - `"switchNote": "El cambio se hace desde Mi cuenta. Antes de confirmar verás el importe exacto que se ajusta en tu próxima factura."`
  - `"guaranteeLead": "Cancela online en dos clics"` y `"guaranteeBody": ", cuando quieras: Mi cuenta → Suscripción → Cancelar. Sin llamadas ni correos."`
- Se borra `trialBadge`.

**Tests:** `checkout.spec.ts` y `screens.spec.ts` asumen Mensual por defecto (`plan-price` y `trial-disclosure`). Actualízalos para que abran en Anual y pulsen «Mensual» antes de comprobar la prueba.

---

# C10 · `/registro` (D4)

**Archivos:** `components/auth/auth-form.tsx`, `app/[locale]/signup/page.tsx`, `messages/*.json`.

- **Titular:** `auth.signupTitle` → «Crea tu cuenta gratis: {words} palabras al día». `signupLede` → «Sin contraseña y sin tarjeta. Entras con Google o con un enlace a tu correo.»
- **Google primero:** botón de 48 px a todo el ancho, con la G oficial en SVG y los colores y el borde de las normas de Google (blanco, borde `#747775`, texto `#1f1f1f`). El separador pasa a «o con tu correo». El botón del correo, a `outline`.
- **Legales como enlaces:** `t.rich("legal", { terms: (c) => <Link href={…terms}>{c}</Link>, privacy: … })` con `"legal": "Al crear la cuenta aceptas los <terms>términos del servicio</terms> y la <privacy>política de privacidad</privacy>."`.
- **«Tu texto sigue en el editor»** solo si `next` apunta a una ruta de herramienta (`/`, `/humanize`, `/detect`… o `/app?tool=…`). Hoy sale siempre.
- **Llegando desde un plan:** `next` empieza por `/checkout` y trae `plan` y `cycle` válidos.
  - Titular: «Primero, tu cuenta. Después, el pago.». Entradilla: «Sin contraseña. En tu cuenta vive la suscripción, y desde ella la cancelas cuando quieras.»
  - Barra superior: pasos «1 Cuenta — 2 Pago».
  - Lateral (≥ 820 px) con «Tu elección», el plan y el ciclo, y las filas Prueba / Hoy pagas / Primer cobro o Renovación / Después. Todo sale de `PRICES`, `trialDaysFor` y hoy + días; son las mismas cifras que `/checkout`.
  - Una nota: con prueba, «Hoy no se te cobra nada. El {fecha} se te cobrarán {importe}/mes salvo que canceles antes, en dos clics desde tu cuenta.»; sin prueba, «Se cobra al confirmar el pago, en el paso siguiente. Nada se cobra al crear la cuenta.».
  - Enlace «Cambiar de plan» → `/pricing?cycle=…`. Por debajo de 820 px, el lateral pasa a una franja compacta sobre el formulario.
  - Se ocultan los beneficios del plan gratis y la línea del texto conservado.
- **Enlace enviado:** se añade la línea «¿No llega en un minuto? Mira en spam o <retry>usa otra dirección</retry>.», que vuelve a enseñar el formulario.
- **Sin campos nuevos ni casilla de marketing.**
- **Eventos:** ya existen (`signup_start` y `signup_done`, con `next`).

**Claves (`auth.*`):** cambian `signupTitle`, `signupLede`, `or` («o con tu correo»), `legal` (rich) y `textKept` («Tu texto sigue en el editor. Al volver lo encuentras tal cual.»). Se añaden `planTitle`, `planLede`, `stepAccount`, `stepPay`, `yourChoice`, `rowTrial`, `rowToday`, `rowFirstCharge`, `rowRenewal`, `rowAfter`, `rowEquivalent`, `planNoteTrial`, `planNoteCharge`, `changePlan`, `notArrived` y `useAnother`. El copy exacto está en `ui_kits/auth/AuthPage.jsx` (`PLAN_COPY`).

---

# C11 · Candados en contexto

- **Cabecera de `/app` (en C7):** en free, «Historial» lleva la insignia «Pro» y abre el `FeatureLock`.
- **Pestañas «Parafraseador» y «Corrector» para free y anónimo:** llevan un «Pro» discreto junto al nombre, para que el muro C no sea una sorpresa. El muro C ya se abre al primer intento de uso.
- Ningún botón queda activo si lleva a un 403 o a un 429 conocidos. El editor ya lo hace con `included`, y C8 lo extiende a la cuota.

# C12 · Ocultar «Crear cuenta gratis» con sesión

`HeaderAuth` ya lo hace en la cabecera. Faltan:
- el pie de marketing (`footer.product` enlaza `/signup` a todo el mundo): hazlo cliente o quítalo;
- la tarjeta Gratis de `/precios` (en C9);
- el `wayOut` del editor, que ya distingue por `plan`, pero en las landings `plan` es siempre `"anonymous"`. Resuelve la sesión en el cliente, como `HeaderAuth`, antes de ofrecer la cuenta.

---

# C13 · Invitación tras el primer resultado (D5)

**Archivos:** `components/tools/tool-editor.tsx`, nuevo `components/billing/signup-invite.tsx`, `lib/analytics/events.ts`, `messages/*.json`.

- **Se muestra** cuando `plan === "anonymous"`, `status === "done"`, no hay `withheld`, `quotaNotice`, `upsell` ni `error`, y `localStorage["vbx:invite:dismissed"]` no existe. Léelo en un efecto, no durante el render (el mismo motivo que `sessionStorage` en el borrador).
- **Posición:** debajo de la tarjeta del editor, fuera de ella. Nunca encima del resultado.
- **Copy:** «**Con una cuenta gratis tienes 500 palabras al día**, no 300.» / «Sin contraseña ni tarjeta: con Google o con un enlace a tu correo. Este resultado se queda aquí.»
- **Acciones:** «Crear cuenta gratis» (`soft sm`) → `/signup?next=<aquí>`, y «Ahora no» (`ghost sm`).
- **Descartar:** escribe la clave en `localStorage` y cambia la tarjeta por «Hecho, no volveremos a mostrarlo. Si cambias de idea, «Crear cuenta gratis» está arriba a la derecha.». Esa línea desaparece en el siguiente `run()`.
- **Eventos:** añade `"invite"` a `WallReason`. Emite `wall_shown { variant: "inline", reason: "invite", plan: "anonymous", tool }` una vez por sesión, y `wall_dismissed { …, method: "x" }` o `{ …, method: "cta" }`.

**Claves (`invite.*`):**
- `"lead": "Con una cuenta gratis tienes {free} palabras al día"`
- `"leadTail": ", no {anon}."`
- `"body": "Sin contraseña ni tarjeta: con Google o con un enlace a tu correo. Este resultado se queda aquí."`
- `"cta": "Crear cuenta gratis"`
- `"dismiss": "Ahora no"`
- `"dismissed": "Hecho, no volveremos a mostrarlo. Si cambias de idea, «Crear cuenta gratis» está arriba a la derecha."`
